import { ComingSoon } from "@/components/shell/ComingSoon";

export default function AgentsPage() {
  return (
    <ComingSoon
      title="Agents"
      icon="spark"
      blurb="Catalog and configuration for every agent — Outreach, Lead Scoring, Scheduler, Forecast, Triage, Onboarding. Model choice, autonomy mode, eval suite, run history, cost ceiling."
      phase="Phase 3 · Control Tower"
    />
  );
}
