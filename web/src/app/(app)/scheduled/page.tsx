import { ComingSoon } from "@/components/shell/ComingSoon";

export default function ScheduledPage() {
  return (
    <ComingSoon
      title="Scheduled"
      icon="clock"
      blurb="Recurring agent jobs and time-fenced work — daily lead-scoring sweeps, weekly forecast refreshes, cohort reminder cascades. Pause, edit, or skip the next run."
      phase="Phase 1 · Sales"
    />
  );
}
