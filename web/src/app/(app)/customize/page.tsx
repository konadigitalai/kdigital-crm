import { ComingSoon } from "@/components/shell/ComingSoon";

export default function CustomizePage() {
  return (
    <ComingSoon
      title="Customize"
      icon="star"
      blurb="Build your own agent: pick a tool palette, set the autonomy mode, write the planner prompt, ship it behind the same approval gate as the built-ins."
      phase="Phase 3 · Action Fabric"
    />
  );
}
 