import { ComingSoon } from "@/components/shell/ComingSoon";

export default function ReportsPage() {
  return (
    <ComingSoon
      title="Reports"
      icon="bars"
      blurb="Pipeline forecast, cohort fill, lead-source ROI, agent cost-per-action, approval acceptance rate. Filterable by program, advisor, and time window."
      phase="Phase 1 · Sales"
    />
  );
}
