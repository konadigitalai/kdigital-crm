import { ComingSoon } from "@/components/shell/ComingSoon";

export default function InboxPage() {
  return (
    <ComingSoon
      title="Inbox"
      icon="inbox"
      blurb="Inbound conversations across WhatsApp, email, and call transcripts — triaged into service cases. Agent-drafted replies waiting for your approval."
      phase="Phase 2 · Service"
    />
  );
}
