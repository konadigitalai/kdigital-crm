# Node
node_modules/
*.log
*.tsbuildinfo
npm-debug.log*

# Next.js
.next/
out/
.vercel/
.next-build/

# Build artifacts
dist/
build/

# Env files (NEVER commit)
.env
.env.local
.env.*.local
api/.env
web/.env.local

# OS
.DS_Store
Thumbs.db
desktop.ini

# Editor
.vscode/
.idea/
*.swp

# Local logs / temp
dev.log
*.log

# Drizzle local snapshots are committed; only ignore studio cache
.drizzle/

# Local scratch files (not part of the repo — don't commit)
*.xlsx
~$*
Product Documents/


# local scratch / probes / dumps — never commit
api/_*.mjs
api/check-prod-state.mjs
api/peek-leads.mjs
api/backups/
api/*.csv
api/import-plan-*.json

.vercel
.env*
